$kind: http-request
order: 10000
method: DELETE
url: '{{baseUrl}}/friends/{{w2ReqId}}'
headers:
  - key: Authorization
    value: 'Bearer {{w2AToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("status is 200", () => pm.response.to.have.status(200));
