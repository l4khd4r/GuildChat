$kind: http-request
order: 1000
method: GET
url: '{{baseUrl}}/me'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("status is 401", () => pm.response.to.have.status(401));
      pm.test("carries an error message", () => {
        pm.expect(pm.response.json().error).to.be.a("string").and.to.not.be.empty;
      });
      pm.test("explains the failure", () => {
        pm.expect(pm.response.json().error).to.include("Authorization header is missing");
      });
