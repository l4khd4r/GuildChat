$kind: collection
name: 'Invalid State Transitions'
description: 'The friendship routes each guard on the current status, so the same id answers differently depending on what state its row is in. These walk one request through every transition.'
