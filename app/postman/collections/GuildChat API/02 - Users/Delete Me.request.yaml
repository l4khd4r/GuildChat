$kind: http-request
order: 6000
method: DELETE
url: '{{baseUrl}}/me'
headers:
  - key: Authorization
    value: 'Bearer {{disposableToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("status is 204", () => pm.response.to.have.status(204));
      pm.test("no content is returned", () => {
        pm.expect(pm.response.text()).to.be.empty;
      });
